/* 

Q1. Print all numbers between 20 and 40 to the console.
Q2. Let's print the even numbers between 0 and 50 to the console.

*/ 

/* A1. */
for (var i = 20; i < 40; i++) {
    console.log(i);
}

/* A2. */
for (var j = 0; j <50; j++) {
    if (j % 2 == 0) {
        console.log(j);
    };
};